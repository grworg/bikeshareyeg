# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Node
node_modules/
.next/
out/

# Data cache (large files)
data/raw/
data/processed/
data/cache/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
.cursor/
.cursorignore
.cursorrules

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
data/gtfs/

# OTP (large graph + OSM data)
data/otp/*.osm.pbf
data/otp/graph.obj
data/otp/gtfs.zip

# Generated overlay data (regenerate with scripts/process-census-data.py)
data/overlays/

# Precomputed hex grid (regenerate with scripts/precompute-hexgrid.py)
data/hexgrid.geojson

# Seed station data (no longer used — sessions start empty)
data/stations.json

# OSMnx road graph cache
data/cache/*.graphml
data/cache/*.pkl

# Overpass API cache (auto-fetched on first run)
data/overpass_cache/

# TypeScript build info
*.tsbuildinfo

# Ansible secrets & real inventory (only .example files are committed)
infrastructure/ansible/inventory/production.yml
infrastructure/ansible/group_vars/production.yml
infrastructure/ansible/group_vars/secrets.yml
